if(impactChecker){
    var impactTitleTimeline = gsap.timeline();

    impactTitleTimeline.from("#impact-title", {duration: 1, alpha: 0, yPercent: -20})
  
}

if(impactChecker){
    var impact2TitleTimeline = gsap.timeline();

    impact2TitleTimeline.from("#impact-sup", {duration: 1, alpha: 0, xPercent: -20})
  
}